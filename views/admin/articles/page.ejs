<%- include ('../../partials/header') %>
<%- include ('../../partials/homenavbar') %>
<body>
	<div class="container">
		<hr>
		<% result.articles.rows.forEach(article => { %>
			<br>
			<div class="card">
				<div class="card-header">
					<h2><%= article.title %></h2>
				</div>
				<div class="card-body">
					<a href="/<%= article.slug %>" class="btn btn-success">Ler Artigo</a>
				</div>
			</div>
		<% }) %>
		<hr>
		<div class="row">
			<div class="col col-12">
				<% if(result.page > 2) { %>
					<a href="/articles/page/<%= result.page - 1 %>" class="btn btn-info" style="float:left"> <- Anterior </a>
				<% }else if(result.page == 2){ %>
					<a href="/"" class="btn btn-info" style="float:left"> <- Anterior </a>
				<% } %>
				
				<% if(result.next) { %>
					<a href="/articles/page/<%= result.page + 1 %>" class="btn btn-info" style="float:right"> Próximo -> </a>
				<% }else{ %>
				<% } %>
			</div>
		</div>
		<hr>
	</div>	
</body>
<%- include ('../../partials/footer') %>